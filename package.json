{
  "name": "sf-boost-chrome",
  "description": "Power tools for Salesforce developers and admins",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "build:reload": "bun run build && bun run scripts/reload-extension.ts",
    "zip": "wxt zip",
    "check": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "wxt": "^0.20.0"
  }
}
